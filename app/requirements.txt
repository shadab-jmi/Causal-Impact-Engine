# Slim runtime dependencies for the Streamlit dashboard (app/streamlit_app.py).
#
# This is the minimal set the app actually imports at runtime — verified that the app's src modules
# pull in no dowhy / jupyter / lightgbm / networkx. Point Streamlit Community Cloud at THIS file for a
# fast, light deploy. The full root requirements.txt reproduces the whole analysis (notebooks + tests).
#
# Versions are pinned to the environment the project was built and verified on (Python 3.12).

streamlit==1.58.0
numpy==2.4.6
pandas==3.0.3
scipy==1.15.3
statsmodels==0.14.6
scikit-learn==1.9.0
scikit-uplift==0.5.1
matplotlib==3.11.0
